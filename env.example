# E2ERep — API keys (copy to .env and fill in your values)
# .env is gitignored — never commit your actual keys

# LLM — at least one required
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Data sources
SEMANTIC_SCHOLAR_API_KEY=
CROSSREF_EMAIL=

# Optional: output directory (default: ./outputs)
# OUTPUT_DIR=./outputs
